{% block extends %}
{{ "{% extends 'base_admin_lte.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block mainTitle %}" }}
    {{ "{% trans %}" }}edit.title.{{ entity_singularized }}{{ "{% endtrans %}" }}
{{ "{% endblock %}" }}
{{ "{% block body %}" }}
<div class="row">
    <div class="col-xs-12 col-lg-6 col-lg-offset-3">
        {{ '{{ form_start(edit_form) }}' }}
        <div class="box box-default color-palette-box">
            <div class="box-header with-border">
                <h3 class="box-title"></h3>
            </div>
            <div class="box-body">
                {{ '{{ form_widget(edit_form) }}' }}
            </div>
            <div class="box-footer">
                <input type="submit" value="{{ "{% trans %}" }}{{ entity_singularized }}.edit{{ "{% endtrans %}" }}" class="btn btn-success"/>
                {% set hide_edit, hide_delete, hide_show = true, false, false %}
                {{ include('crud/views/others/record_actions.html.twig.twig') -}}
            </div>
        </div>
        {{ '{{ form_end(edit_form) }}' }}
    </div>
</div>

{{ "{% endblock %}" }}
{{ "{% block javascript %}
    {{ parent() }}
    <script src=\"{{ asset('adminlte/js/run/run-datepicker.js') }}\"></script>
    <script src=\"{{ asset('adminlte/js/run/run-datetimepicker.js') }}\"></script>
    <script src=\"{{ asset('adminlte/js/run/run-wysihtml5.js') }}\"></script>

{% endblock %}" }}
{% endblock body %}
